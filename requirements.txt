# 律师介绍系统 — Python 依赖清单
# 仅包含项目实际使用的包，定期用 pip-audit 检查安全漏洞

Django==6.1
django-simpleui==2026.1.13
djangorestframework==3.18.0
django-cors-headers==4.9.0
python-decouple==3.8
Pillow==12.3.0

# 生产环境依赖（开发环境可不安装）
gunicorn>=22.0          # WSGI 服务器
whitenoise>=6.7         # 静态文件服务
psycopg2-binary>=2.9    # PostgreSQL 驱动（使用 DATABASE_URL 时必需）
